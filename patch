src/interp/msgdb.lisp remove dead code, simplify 

Certain functions were never used or used a few times.
These were removed and code elsewhere rewritten.

In particular, centering code can be done directly by
common lisp format strings so a lot of this was removed.






