books/bookvolbug bug 7324: MATLIN fractionFreeGauss! elt error

Goal: Bug fix

Barry M Trager
      
Tim, I found and fixed a bug in determinants of zero matrices over 
finite fields.

if you do:
zero(2,2)$Matrix(POLY PF 2)
determinant %

you get an elt index error

the fix is to change 4th from last line the function fractionFreeGauss! of MatrixLinearAlgebraFunctions

from:

        if ans=-1 then

to:

        if ans=-1 and i>minR then
